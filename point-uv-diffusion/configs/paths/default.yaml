
root_dir: ${oc.env:PROJECT_ROOT}

# path to (preprocessed) data folder

# ------------ paths for 3d future

# raw_data_dir: /scratch/leuven/375/vsc37593/3D-FUTURE-model

# data_dir: /scratch/leuven/375/vsc37593/3D-FUTURE-Preprocessed-24Views_copy

# flag_csv: /data/leuven/375/vsc37593/my_py_projects/Future3d-24views-flag.csv

# ------------ paths for shapenet

raw_data_dir: /scratch/leuven/375/vsc37593/ShapeNet_raw

data_dir: /scratch/leuven/375/vsc37593/ShapeNet-Preprocessed

flag_csv: /data/leuven/375/vsc37593/my_py_projects/shapenet-flag.csv


# path to logging directory
log_dir: ${paths.root_dir}/logs

# path to output directory, created dynamically by hydra
# store all files generated during the run, like ckpts and metrics
exp_save_dir: ${paths.root_dir}/results

# Whether to include timestamp in the directory name
include_timestamp: False

# Directory names with and without timestamp
dir_name_with_timestamp: ${now:%Y-%m-%d}_${now:%H-%M-%S}
dir_name_without_timestamp: static_timestamped

# New field that uses the new resolver
dir_name: ${dir_resolver:${paths.include_timestamp},${paths.dir_name_with_timestamp},${paths.dir_name_without_timestamp}}

# path to output directory, created dynamically by hydra
output_dir: ${paths.exp_save_dir}/${task_name}/${post_tag}/${paths.dir_name}

# path to test results
test_output_dir: ${paths.exp_save_dir}/test/${exp_name}/${paths.dir_name}